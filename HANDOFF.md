# HANDOFF.md — Canvas Vivo ATDG

Arquivo de continuidade entre sessões e entre IAs (Claude, GPT, Gemini), conforme o Protocolo de Trabalho — Claude. Leia este arquivo antes de iniciar qualquer trabalho neste projeto.

---

## Última atualização

- **Data:** 2026-08-11
- **IA utilizada:** Claude (Sonnet 5)
- **Tarefa realizada:** Criação da estrutura inicial de memória externa persistente do projeto: `PROJECT_CONTEXT.md`, `HANDOFF.md` e `CHANGELOG.md`.
- **Resultado:** Estrutura criada com sucesso. Conteúdo baseado no `README.md` existente e na listagem de arquivos do repositório; nenhuma informação foi inventada além do que já constava no repositório.
- **Decisões:**
  - Adotar este conjunto de três arquivos como memória externa persistente do projeto, conforme o protocolo.
  - Não alterar `index.html`, `data.json` ou o `.docx` institucional nesta etapa — apenas criar a camada de documentação/protocolo.
- **Arquivos alterados:**
  - `PROJECT_CONTEXT.md` (novo)
  - `HANDOFF.md` (novo)
  - `CHANGELOG.md` (novo)
- **Pendências:**
  - Confirmar com o usuário a URL real de publicação (GitHub Pages) para atualizar o placeholder `SEU-USUARIO` no `README.md`, se aplicável.
  - Não há SKILLS específicas do projeto identificadas neste repositório ainda — avaliar se serão criadas/adicionadas futuramente.
  - Revisar se há decisões metodológicas anteriores (da v1 à v4 do Canvas) que devam ser documentadas retroativamente, caso o usuário forneça esse contexto.
- **Próxima etapa:** Aguardar indicação do usuário sobre a próxima tarefa concreta no projeto (ex.: ajuste no `index.html`, no `data.json`, ou definição de SKILLS específicas).

---

## Como preencher este arquivo em atualizações futuras

Ao final de cada etapa relevante de trabalho, adicionar uma nova seção acima desta, seguindo o mesmo formato: data, IA utilizada, tarefa realizada, resultado, decisões, arquivos alterados, pendências, próxima etapa. Manter o histórico completo (não sobrescrever entradas anteriores).
